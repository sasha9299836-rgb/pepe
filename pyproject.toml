[project]
name = "games-project-lazarenko"
version = "0.3.0"
description = "вд-1"
authors = [
    { name = "Arseniy Lazarenko", email = "sasha9299836-rgb@gmail.com" },
]
dependencies = [
    "prompt>=0.4.1",
]
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]

[project.scripts]
games-app = "games_project_lazarenko.scripts.main:main"
VD-even = "games_project_lazarenko.scripts.even:main"
